[package]
name = "arachne-input"
version = "0.1.0"
edition.workspace = true

[dependencies]
arachne-math = { path = "../arachne-math" }
winit = { version = "0.30", optional = true }

[features]
windowed = ["winit"]

[dev-dependencies]
