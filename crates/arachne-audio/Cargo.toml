[package]
name = "arachne-audio"
version = "0.1.0"
edition.workspace = true

[dependencies]
arachne-math = { path = "../arachne-math" }
cpal = { version = "0.15", optional = true }

[features]
native-audio = ["cpal"]

[dev-dependencies]
